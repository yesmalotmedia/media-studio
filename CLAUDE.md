# CLAUDE.md — סטודיו שיעורים

קובץ הקשר להמשך עבודה ב-Claude Code. קרא אותו לפני שאתה נוגע בקוד.

## מה זה הפרויקט

מערכת מקומית (Next.js 14, App Router) לעיבוד שיעורי תורה מצולמים:
מזהה שיעורים בתוך גלמי וידאו ארוכים לפי פס הקול, מציגה רשימת חיתוכים
לאישור אנושי, מאפשרת תיקון מדויק בעורך מובנה, ובהמשך תחתוך/תנרמל/
תזום/תייצא ותעלה ליוטיוב.

רץ על **Windows**, Node על שרת מקומי (localhost:3000). הגלמים על
שרת רשת: `\\Servermedia\הקלטות\...` (נתיבי UNC, שמות קבצים בעברית).

## סטטוס נוכחי (מה עובד)

1. **עורך חיתוך ידני** — נגן HTML5 + waveform + סמני in/out + מקשי
   קיצור (I/O/רווח/חיצים) + ייצוא ffmpeg. עובד.
2. **זיהוי אוטומטי (VAD)** — סורק תיקייה, מזהה מקטעי שיעור לפי עוצמת
   קול, מפרק לרשימת חיתוכים שטוחה. **עודכן** להיסטרזיס+דה-באונס
   (ראה "כיול היסטרזיס" למטה) — נבדק ואומת על גלם מלא אמיתי.
3. **מסך אישור מבוסס-חיתוכים** — כל שורה = שיעור מועמד (תמונה, זמנים,
   ודאות ירוק/צהוב). אשר / דחה / תקן. "תקן" פותח את העורך. עובד.

## ארכיטקטורה

- `lib/detect.js` — מנוע הזיהוי (**היסטרזיס + דה-באונס**, ראה "כיול
  היסטרזיס" למטה לפרטים ולמדידות שהובילו לזה):
  - `rmsProfile(file, step)` — עוצמת RMS לכל חלון (שנייה).
  - `speechLevels(file, step, {openFraction, continueFraction})` — מחשב
    rms + שני ספים יחסית לרמת הדיבור החזק (אחוזון 90): `openThr` (גבוה,
    strong*openFraction) ו-`continueThr` (נמוך בהרבה, strong*continueFraction).
  - `runsFromLevels(rms, step, {openThr, continueThr, bridgeSec, minRunSec,
    minSpeechRunSec})` — בונה רצפים בהיסטרזיס: *פותח* רצף רק מעל openThr,
    אבל ברגע שנפתח — נשאר פתוח כל עוד יש פעילות מעל continueThr. כדי
    שרעש בודד (חריקת כיסא וכו') לא ישאיר רצף פתוח על חדר ריק, נדרש רצף
    של `minSpeechRunSec` שניות רצופות מעל continueThr כדי "לאשר" חידוש
    (דה-באונס) — ראייה בודדת לא מספיקה לאיפוס השקט.
  - `detectSegments(file, opts)` — מלא: מחזיר segments עם pad ודירוג ודאות.
- `lib/store.js` — אחסון JSON (data/segments.json) של סריקות + segments.
- `lib/ff.js` — עטיפות ffmpeg. **חשוב:** ב-Windows כל קריאה עוברת דרך
  `cmd /c chcp 65001` + windowsVerbatimArguments — זה פותר שמות עבריים
  ו-UNC. אל תשבור את זה.
- `lib/config.js` — config + listVideos + encode/decodePath (base64url).
- `app/api/scan` — סריקת רקע של תיקייה (קורא detect params מ-config.json).
- `app/api/segment` — עדכון segment (אשר/דחה/עריכת start-end).
- `app/api/media/[id]` — streaming עם HTTP range + retry על EBUSY (UNC).
- `app/api/analyze/[id]` — waveform peaks (cache ב-data/cache).
- `app/api/thumb/[id]` — תמונה ממוזערת בזמן t.
- `app/page.js` — מסך רשימת החיתוכים + עורך מובנה.

## פרמטרי כיול (config.json → detect)

```
openFraction: 0.5        # סף פתיחה = 50% מרמת הדיבור החזק. רק זה פותח שיעור חדש.
continueFraction: 0.15   # סף המשך, נמוך בהרבה. שומר שיעור פתוח שכבר התחיל.
minSpeechRunSec: 3       # כמה שניות רצופות מעל continueThr נדרשות כדי "לאשר" חידוש (דה-באונס)
bridgeSec: 30            # שקט קצר מזה לא שובר שיעור (לא מצטבר מעבר לזה)
minRunSec: 90            # רצף מתחת ל-90 שנ' לא נחשב שיעור
pad: 1.0                 # שנייה לפני/אחרי כל חיתוך
```

### מספרי כיול שנמדדו (עוצמות RMS x1000)

מדגימה קטנה (test3.mp4):
- דיבור-שיעור (מול מיקרופון): 90-170
- שני אנשים קרוב, לא מול המיקרופון (**לא** שיעור): 26-39
- רקע / דיבור חלש: 0-10

**מגלם מלא אמיתי (5.5 שעות, `\\Servermedia\הקלטות\test\עתים 31-8.mp4`,
2026-09-02)** — זה מה שהוביל למספרים הנוכחיים ב-config.json:
- רקע/שתיקה אמיתית בחדר ריק: 0-10, עם ניתורים מבודדים (שנייה אחת)
  שמגיעים ל-15-25 ולעיתים אף לספיקים גבוהים בודדים (חריקת כיסא וכו').
- הרב מדבר "חלש"/לא ישר מול המיקרופון אך **באמצע שיעור רציף**: 10-60
  בערך, ברצף כמעט-רציף — הפער הארוך ביותר בין דגימות מעל הסף היה 3-7
  שניות בכל הפערים הכוזבים שנבדקו.
- דיבור חזק מול-מיקרופון: 45-200+.
- ההבדל הקריטי בין "שתיקה אמיתית עם ניתורים בודדים" ל"דיבור חלש רציף"
  הוא לא העוצמה (שתיהן חופפות ב-10-40) אלא **הצפיפות בזמן**: ניתור
  אמיתי הוא בודד (שנייה אחת, מוקף בשתיקה), דיבור הוא רצוף. זו הסיבה
  ל-minSpeechRunSec.

## באגים — סטטוס אחרי מדידה ותיקון על גלם מלא (2026-09-02)

1. **פיצול באמצע שיעור — תוקן, אומת.** שיעור אחד נחתך ל-2-3 חלקים כי
   סף יחיד (0.5) נופל בדיוק בתוך טווח "דיבור חלש רציף" (10-40), אז כל
   ירידה טבעית בעוצמה שנמשכת קצת מעל bridgeSec שוברת את הרצף. **תוקן**
   עם היסטרזיס (openThr גבוה לפתיחה + continueThr נמוך להמשך) — ראה
   `runsFromLevels` ב-`lib/detect.js`. על הגלם שנמדד: 8 מקטעים (עם
   פיצולים כוזבים) → 4 מקטעים נכונים אחרי התיקון.
2. **בליעת "שתיקה" ארוכה בגלל היסטרזיס לבדו — התגלה ותוקן.** אחרי
   התיקון הראשון (היסטרזיס בלי דה-באונס) התגלתה תופעת-לוואי: סף המשך
   נמוך (0.15) התאפס גם על ניתור-רעש בודד של שנייה אחת בחדר ריק, אז
   מקטע "נשאר פתוח" ובלע 6.5 דקות של חדר ריק אחרי סוף השיעור בפועל.
   **תוקן** עם `minSpeechRunSec=3` (דה-באונס: נדרש רצף, לא דגימה בודדת,
   כדי לאפס את מונה השקט) — צמצם את הבליעה מ-6.5 דקות ל-~10 שניות
   (כמעט זהה לגבול המקורי הנכון).
3. **התחלה/סוף בולעים דקה של פטפוט** — **נבדק פעם אחת, לא נצפה** בגבול
   שנבדק (תחילת השיעור הראשון בגלם שנמדד הייתה נקייה — קפיצה חדה
   משתיקה 0-18 לדיבור חזק 49-67, בלי פטפוט-מקדים שנבלע). ייתכן שהבאג
   קיים בגלמים/גבולות אחרים שלא נבדקו — לא סגור סופית, רק לא שוחזר.

**הצעד הבא:** לפתוח את מסך האישור (localhost) ולעבור ידנית/אוזנית על
4 המקטעים שהתקבלו מ-`עתים 31-8.mp4`, לוודא שהגבולות נכונים גם
בהאזנה בפועל (לא רק ב-RMS). אם יתגלו עוד גלמים עם פיצול/בליעה למרות
התיקון — למדוד RMS גולמי סביב הגבול הבעייתי (כמו שנעשה כאן) לפני שינוי
נוסף בספים, לא לכייל בעיוור.

## מפת דרכים

1. ~~כיול היסטרזיס על גלמים מלאים~~ — בוצע, ראה "באגים" למעלה.
   הבא בתור: אימות אוזנית על התוצאה + כיול על עוד גלמים אם צריך.
2. **זום אוטומטי** — כבר הוכח: זיהוי ראש (OpenCV/פרופיל) + קו שולחן →
   קרופ קבוע עם 10% אוויר מעל הראש, 16:9. הכלל: הגובה המקסימלי של
   הראש לאורך השיעור קובע את הקרופ. צריך מימוש ב-Node/Windows בלי
   תלויות native שבירות (onnxruntime בעייתי בהתקנה).
3. **עיבוד המאושרים** — חיתוך (+1 שנ') → נורמליזציה (loudnorm) → זום →
   פייד → ייצוא 16:9. cutSegment ב-ff.js כבר תומך בנורמליזציה+פייד.
4. **דליברי** — כותרות מטבלה (CSV/Excel), שיוך תיקיות ייצוא, פורמט זהה
   ליוטיוב וספוטיפיי.
5. **יוטיוב** — העלאה דרך YouTube Data API (רשמי, לא הדמיית דפדפן).

## החלטות מוצר שכבר סוכמו

- קרופ **קבוע** לכל שיעור (לא דינמי) — לפי הגובה המקסימלי של הראש.
- אותו פורמט 16:9 גם ליוטיוב וגם לספוטיפיי (וידאו).
- ספוטיפיי: הועלה בעבר דרך Spotify for Podcasters. אין API רשמי
  להעלאה; feed בניהול-עצמי דורש מעבר hosting. **לא הוחלט סופית** —
  כרגע המיקוד בעיבוד (זיהוי/חיתוך/זום), לא בהעלאה לספוטיפיי.
- מסך האישור האנושי הוא חלק מהתכנון — המערכת תופסת "יותר מדי" בכוונה,
  והמשתמש מסנן. עדיף מזה שתפספס שיעורים.

## הרצה

```
npm install
npm run dev   # localhost:3000
```
צריך ffmpeg + ffprobe ב-PATH (או נתיב מלא ב-config.json).

## כללי עבודה למי שממשיך (Claude Code)

- **אל תשבור** את טיפול ה-UTF-8/UNC ב-ff.js (chcp 65001 + verbatim args).
- מדוד על גלמים אמיתיים לפני שינוי ספים — אל תכייל בעיוור.
- שמור על עקרון "זיהוי + אישור אנושי", לא אוטומציה עיוורת.
- בדוק build (`npm run build`) אחרי שינויים בקוד.
